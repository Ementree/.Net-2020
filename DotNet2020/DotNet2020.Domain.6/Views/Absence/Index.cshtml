@{
    Layout = "~/Views/Shared/_Layout6.cshtml";
}
@using DotNet2020.Domain._6.Models.ViewModels
@model AbsencesViewModel
<!DOCTYPE html>
    <div class="container-fluid mb-3 d-inline-flex">
        <form asp-action="Index" class="m-2 p-2">
        </form>
    </div>
    <div class="wrapper">
        <div class="scroller1">
            <table class="scroll-table table">
                <thead>
                    <tr>
                        <th class="sticky-col1">  </th>
                        @foreach (var period in Model.Months)
                        {
                            <td colspan="2">@period</td>
                        }
                    </tr>
                </thead>
                <tbody>
                    @foreach (var resource in Model.ResourceAbsences)
                    {
                        <tr>
                            <td class="sticky-col1">@resource.Key</td>
                            @for (int i = 0; i < resource.Value.Item1.Count; i++)
                            {
                                if (resource.Value.Item1[i])
                                {
                                    <td colspan="2" bgcolor="#FF0000">@resource.Value.Item2[i]</td>
                                }
                                else
                                {
                                    <td colspan="2">@resource.Value.Item2[i]</td>
                                }
                            }

                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
